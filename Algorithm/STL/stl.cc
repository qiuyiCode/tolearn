#include <iostream>
#include <algorithm>
#include <cstring>
#include <cstdio>
#include <set>
#include <queue>
#include <vector>
#include <bitset>
using namespace std;

int main(){
  // priority_queue<int,vector<int>,greater<int>> heap; // 小根堆,默认是大根堆

  // set<int> set;
  // set.insert(1);
  // set.insert(2);
  // set.insert(3);
  // set.insert(4);
  // set.insert(5);
  // cout << *set.lower_bound(3); // 返回第一个>=3的数
  // cout << *set.upper_bound(3); // 返回第一个>3的数

  bitset<10000> s; // 按位存储操作的容器
  cout << s.none();

}
